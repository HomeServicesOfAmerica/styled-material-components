import React from 'react';
import styled from 'styled-components';
import { Icon } from '../icons';

export const PriorityHighIcon = styled(props => (
  <Icon {...props}>
    <path d="M10 3h4v12h-4z" key='path0' />
  </Icon>
))``;
