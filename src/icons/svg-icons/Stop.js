import React from 'react';
import styled from 'styled-components';
import { Icon } from '../icons';

export const StopIcon = styled(props => (
  <Icon {...props}>
    <path d="M6 6h12v12H6z" key='path0' />
  </Icon>
))``;
